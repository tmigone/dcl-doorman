{
  "name": "dcl-doorman",
  "version": "0.0.1",
  "description": "A doorman for my decentraland estate",
  "scripts": {
    "start": "dcl start",
    "build": "build-ecs",
    "watch": "build-ecs --watch",
    "deploy:now": "dcl export && now export",
    "ecs:install": "npm install --save-dev decentraland-ecs@latest",
    "ecs:install-next": "npm install --save-dev decentraland-ecs@next"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "axios": "^0.20.0",
    "decentraland-ecs": "latest"
  },
  "dependencies": {
    "decentraland-ecs-utils": "^1.1.7"
  }
}
